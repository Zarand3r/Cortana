<?xml version="1.0" encoding="UTF-8"?>
<!-- Hardened-runtime entitlements for the notarized Cortana.app.
     MLX compiles/loads Metal kernels at runtime, so it needs the JIT + unsigned-
     executable-memory allowances; disable-library-validation lets the bundled
     mlx/pyobjc dylibs (signed by us, not Apple) load under the hardened runtime. -->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
</dict>
</plist>
